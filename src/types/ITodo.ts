export interface INewTodo {
  title: string;
  text: string;
  endDate: string;
}

export default interface ITodo {
  id: number;
  title: string;
  text: string;
  createdAt: string;
  endDate: string;
}
